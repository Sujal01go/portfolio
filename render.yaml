services:
  - type: web
    name: sujal-portfolio
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    staticSiteDir: dist
    envVars:
      - key: NODE_VERSION
        value: 18
